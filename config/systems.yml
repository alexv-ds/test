groups:
  engine-update:
    - InputWASDPositionController
    - SyncCameraWithMainWindow
    - UpdateBBox
    - UpdateMap
  engine-draw-begin:
    - RenderSokolBegin
    - RenderSokolDraw
  engine-draw-end:
    - RenderSokolEnd
  test:
    - scaler
    - rotator

order:
  - group.test
  - intersect_colorizer
  - group.engine-update
  - group.engine-draw-begin
  - group.engine-draw-end

